# Server Configuration
server.port=3000

# Database Configuration
# IMPORTANT: Update these values with your actual database credentials

# For Railway MySQL Cloud Database (ACTIVE):
# Using PUBLIC URL for connection from local machine
# Public URL: mysql://root:***@shuttle.proxy.rlwy.net:36089/railway
spring.datasource.url=jdbc:mysql://shuttle.proxy.rlwy.net:36089/railway?useSSL=true&requireSSL=true&allowPublicKeyRetrieval=true&useAffectedRows=true&useUnicode=true&characterEncoding=utf8
spring.datasource.username=root
spring.datasource.password=YSjgQqunLfGCFLBqYoQusHqmqXuSMJUf

# For Local MySQL Database (DISABLED):
# spring.datasource.url=jdbc:mysql://localhost:3306/term_project
# spring.datasource.username=root
# spring.datasource.password=your_mysql_password_here

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
# Use 'update' for auto-creating tables, 'validate' for production
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
# Use custom naming strategy to preserve case sensitivity
spring.jpa.properties.hibernate.physical_naming_strategy=com.quadgrimoire.config.CaseSensitiveNamingStrategy

# Session Configuration
spring.session.store-type=jdbc
spring.session.jdbc.initialize-schema=always
spring.session.timeout=86400

# Email Configuration
# Gmail SMTP Configuration
# spring.mail.username = อีเมล Gmail ที่ใช้ส่งอีเมล (ต้องเป็นอีเมลเดียวกับที่สร้าง App Password)
# spring.mail.password = App Password (16 หลัก ไม่มีช่องว่าง)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=khimmyy12@gmail.com
spring.mail.password=sooxqabskujbeftl
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# Contact Email Configuration
# app.contact.email = อีเมลที่รับอีเมลจาก contact form (อีเมล admin)
app.contact.email=khimmyy12@gmail.com
